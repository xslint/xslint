# SPDX-FileCopyrightText: Copyright (c) 2025-2026 Max Trunnikov
# SPDX-License-Identifier: MIT
---
pack: scans-whole-document
# A scan from the root costs what it costs whichever attribute carries the
# expression. The `use` of an `xsl:key` is evaluated for every node the key
# matches, the `test` of an `xsl:if` for every node the template is applied
# to, the `group-by` for every item of the population, and the braces of a
# literal result element every time the element is instantiated.
found:
  amount: 4
  positions: [ [ 4, 40 ], [ 6, 19 ], [ 9, 49 ], [ 12, 15 ] ]
  fixes: [ null, null, null, null ]
input: |-
  <?xml version="1.0"?>
  <xsl:stylesheet xmlns:xsl="http://www.w3.org/1999/XSL/Transform" version="2.0" id="style1">
    <xsl:output encoding="UTF-8" method="xml"/>
    <xsl:key name="by-id" match="o" use="//index/@id"/>
    <xsl:template match="/objects/o">
      <xsl:if test="//o[@pipe]">
        <xsl:value-of select="."/>
      </xsl:if>
      <xsl:for-each-group select="item" group-by="//category/@name">
        <xsl:value-of select="."/>
      </xsl:for-each-group>
      <out id="{//first/@id}"/>
    </xsl:template>
  </xsl:stylesheet>
