# SPDX-FileCopyrightText: Copyright (c) 2025-2026 Max Trunnikov
# SPDX-License-Identifier: MIT
---
pack: unused-function-template-parameter
found:
  amount: 1
  positions: [ [ 8, 5 ] ]
  fixes: [ null ]
input: |-
  <?xml version="1.0"?>
  <xsl:stylesheet xmlns:xsl="http://www.w3.org/1999/XSL/Transform" version="2.0">
    <xsl:output encoding="UTF-8" method="xml"/>
    <xsl:template name="deep">
      <xsl:param name="alpha"/>
      <xsl:param name="beta"/>
      <xsl:param name="gamma"/>
      <xsl:param name="delta"/>
      <xsl:value-of select="item[@id = $alpha]/name"/>
      <xsl:value-of select="concat('x', substring($beta, 2, 3))"/>
      <xsl:if test="$gamma and true()">
        <para>ok</para>
      </xsl:if>
    </xsl:template>
  </xsl:stylesheet>
