# SPDX-FileCopyrightText: Copyright (c) 2025-2026 Max Trunnikov
# SPDX-License-Identifier: MIT
---
pack: count-compared-to-zero
# The prefix reads like the standard one and is bound elsewhere, so the call is
# a user function and the check stays quiet (#577).
found:
  amount: 0
  positions: [ ]
  fixes: [ ]
input: |-
  <?xml version="1.0"?>
  <xsl:stylesheet xmlns:xsl="http://www.w3.org/1999/XSL/Transform" xmlns:fn="urn:mine" version="2.0" id="s">
    <xsl:template match="/">
      <xsl:variable name="a" select="fn:count(x) = 0"/>
    </xsl:template>
  </xsl:stylesheet>
