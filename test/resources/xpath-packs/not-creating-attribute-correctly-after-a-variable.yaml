# SPDX-FileCopyrightText: Copyright (c) 2025-2026 Max Trunnikov
# SPDX-License-Identifier: MIT
---
pack: not-creating-attribute-correctly
# Reported, an xsl:variable binding a value and writing nothing into the
# element it stands in, so the xsl:attribute behind it is still the first
# thing the element is given and the inline form says what this says (#950),
# as long as the value reads no variable the start tag cannot see (#1005).
found:
  amount: 1
  positions: [ [ 7, 7 ] ]
input: |-
  <?xml version="1.0"?>
  <xsl:stylesheet xmlns:xsl="http://www.w3.org/1999/XSL/Transform" version="2.0" id="style1">
    <xsl:output encoding="UTF-8" method="xml"/>
    <xsl:template match="/objects/o">
      <iota>
        <xsl:variable name="way" select="'ltr'"/>
        <xsl:attribute name="dir">
          <xsl:value-of select="'ltr'"/>
        </xsl:attribute>
      </iota>
    </xsl:template>
  </xsl:stylesheet>
