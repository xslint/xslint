# SPDX-FileCopyrightText: Copyright (c) 2025-2026 Max Trunnikov
# SPDX-License-Identifier: MIT
---
pack: not-creating-attribute-correctly
# The first of the two is reported and the second is not, an xsl:attribute
# repeating a name overriding the one in front of it where the inline form it
# would become is overridden instead; nor is one standing behind a name no
# selector can read, an attribute value template naming whatever it evaluates
# to and a shadow attribute naming it somewhere else (#950).
found:
  amount: 1
  positions: [ [ 6, 7 ] ]
input: |-
  <?xml version="1.0"?>
  <xsl:stylesheet xmlns:xsl="http://www.w3.org/1999/XSL/Transform" version="2.0" id="style1">
    <xsl:output encoding="UTF-8" method="xml"/>
    <xsl:template match="/objects/o">
      <zeta>
        <xsl:attribute name="rev">one</xsl:attribute>
        <xsl:attribute name="rev">two</xsl:attribute>
      </zeta>
      <eta>
        <xsl:attribute name="{@hop}">one</xsl:attribute>
        <xsl:attribute name="rel">two</xsl:attribute>
      </eta>
      <theta>
        <xsl:attribute _name="{@hop}">one</xsl:attribute>
        <xsl:attribute name="src">two</xsl:attribute>
      </theta>
    </xsl:template>
  </xsl:stylesheet>
