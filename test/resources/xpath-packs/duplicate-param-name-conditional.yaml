# SPDX-FileCopyrightText: Copyright (c) 2025-2026 Max Trunnikov
# SPDX-License-Identifier: MIT
---
pack: duplicate-param-name
found:
  amount: 0
  positions: [ ]
input: |-
  <?xml version="1.0"?>
  <xsl:stylesheet xmlns:xsl="http://www.w3.org/1999/XSL/Transform" version="3.0" id="style1">
    <xsl:output encoding="UTF-8" method="xml"/>
    <xsl:template name="render">
      <xsl:param name="width" use-when="system-property('xsl:version') = '3.0'"/>
      <xsl:param name="width" use-when="system-property('xsl:version') != '3.0'"/>
      <xsl:sequence select="$width"/>
    </xsl:template>
    <xsl:template name="shadowed">
      <xsl:param name="height" _use-when="system-property('xsl:vendor') = 'Saxonica'"/>
      <xsl:param name="height" use-when="system-property('xsl:vendor') != 'Saxonica'"/>
      <xsl:sequence select="$height"/>
    </xsl:template>
  </xsl:stylesheet>
