# SPDX-FileCopyrightText: Copyright (c) 2025-2026 Max Trunnikov
# SPDX-License-Identifier: MIT
---
pack: empty-choose
found:
  amount: 3
  positions: [ [ 5, 5 ], [ 13, 5 ], [ 21, 5 ] ]
input: |-
  <?xml version="1.0"?>
  <xsl:stylesheet xmlns:xsl="http://www.w3.org/1999/XSL/Transform" version="3.0" id="style1">
    <xsl:output encoding="UTF-8" method="xml"/>
    <xsl:template match="/objects">
      <xsl:choose>
        <xsl:when use-when="false()" test="@a">
          <p>a</p>
        </xsl:when>
        <xsl:otherwise>
          <p>b</p>
        </xsl:otherwise>
      </xsl:choose>
      <xsl:choose>
        <xsl:when _use-when="{'false()'}" test="@a">
          <p>c</p>
        </xsl:when>
        <xsl:otherwise>
          <p>d</p>
        </xsl:otherwise>
      </xsl:choose>
      <xsl:choose>
        <xsl:when use-when="false()" test="@a">
          <p>e</p>
        </xsl:when>
        <xsl:when use-when="false()" test="@b">
          <p>f</p>
        </xsl:when>
        <xsl:when use-when="false()" test="@c">
          <p>g</p>
        </xsl:when>
        <xsl:otherwise>
          <p>gg</p>
        </xsl:otherwise>
      </xsl:choose>
      <xsl:choose>
        <xsl:when use-when="false()" test="@a">
          <p>h</p>
        </xsl:when>
        <xsl:when test="@b">
          <p>i</p>
        </xsl:when>
        <xsl:otherwise>
          <p>ii</p>
        </xsl:otherwise>
      </xsl:choose>
    </xsl:template>
  </xsl:stylesheet>
